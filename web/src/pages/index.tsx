import type { NextPage } from "next";
import Head from "next/head";

const Home: NextPage = () => {
  return (
    <Head>
      <title>Edwces</title>
      <meta name="description" content="Edwces website" />
      <link rel="icon" href="/favicon.ico" />
    </Head>
  );
};

export default Home;
